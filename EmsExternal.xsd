<?xml version="1.0" encoding="UTF-8"?>

<!-- This schema exports the subset of EMS types for use in external schemas. -->
<xs:schema  xmlns:xs="http://www.w3.org/2001/XMLSchema"
			targetNamespace="http://esa.egos.ems" elementFormDefault="qualified">
    <xs:import schemaLocation="EmsTypes.xsd"/>

	<xs:element name="configuration_profile" type="parameter_configuration_profile"/>

</xs:schema>
